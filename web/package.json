{
  "name": "kirchhoff-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-wasm": "cp ../build-wasm-ng/kirchhoff.js public/",
    "dev": "npm run sync-wasm && vite",
    "build": "npm run sync-wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.2"
  }
}
